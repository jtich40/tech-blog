<div>
  {{#each posts as |post|}}
  <div>
    <h3>{{post.title}}</h3>
    <span><a href="/{{post.id}}">View Blog</a></span>
    <div>
      <p>{{post.content}}</p>
      <p>Written by {{user.username}} on {{formatDate created_at}}</p>
    </div>
  </div>
{{/each}}
</div>
